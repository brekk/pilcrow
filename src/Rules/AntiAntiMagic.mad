import String from "String"

import Rule from "spirit-gum/Rule"
import LTransformer from "spirit-gum/Rule/TransformerLine"

import { CONSTANTS } from "@/Constants"



export rule = Rule.lineRule(
  false,
  "pilcrow/markdown/anti-anti-magic",
  [],
  [LTransformer.ofLine((_, _, x) => String.replace(CONSTANTS.ESCAPE_ESC_MADLIB, "$__MADLIB__", x))],
)
